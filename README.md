
  # Super-Calc UI Prototype

  This is a code bundle for Super-Calc UI Prototype. The original project is available at https://www.figma.com/design/y9zeEkqRmjaBQDTgIyn43R/Super-Calc-UI-Prototype.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  